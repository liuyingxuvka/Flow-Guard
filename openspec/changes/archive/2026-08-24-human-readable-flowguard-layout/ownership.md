# Current execution ownership

This change is owned by the primary execution thread (`/root`) for the current
direct replacement work. The untracked draft
`flowguard/project_layout.py` is claimed exclusively by that owner for the
layout-contract work in this change.

The claim was made after a read-only review of the repository coordination
files and the draft. The frozen pre-edit SHA-256 is:

`108b6e078b2089faa7a8edfbd49d5caf8d5196a1dbe0c6b389b6a8c03d1c8a32`

No other agent may edit `project_layout.py`. Reverse-surface work is separately
limited to `flowguard/behavior_surface_audit.py` and its tests and does not
overlap this claim.

This ownership record is coordination evidence only. It is not a model
authority, runtime reader, compatibility route, migration instruction, or
completion receipt.
